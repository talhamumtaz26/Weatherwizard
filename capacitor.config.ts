import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.weatherwizard.app',
  appName: 'WeatherWizard',
  webDir: 'dist/public',
};

export default config;
